---
- hosts: all
  tasks:
    - name: Get information
      shell: /usr/bin/df -ih | grep -ohe '%' | sed 's/,//g' | awk '{ print $1" "$4 }'
      changed_when: False
      register: inodes
  
    - debug:
        msg: "{{ inodes }} available on {{ HOSTNAME }}"
